import React from "react";

const DeleteGame = ({ game }) => {
    // console.log(game);
    const { id, player_id } = game;

    // deleteGameFromHistory(id, player_id);

    return "Delete game from history";
    //ask for confirmation
};

export default DeleteGame;
